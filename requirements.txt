# Soul OS — Python dependencies
# Phase 1 必要
pydantic>=2.0
httpx>=0.27

# 配置載入
python-dotenv>=1.0
pyyaml>=6.0

# SAGE-lite 圖譜記憶（Phase 2）
networkx>=3.0

# I/O Gateway（Phase 4 — WebSocket）
fastapi>=0.110.0
websockets>=12.0
uvicorn[standard]>=0.29.0

# 開發/測試（optional）
# pytest>=7.0
# pytest-asyncio>=0.21
# Phase 6.x TTS �X msedge-tts (Edge neural voices)
edge-tts>=7.0


# M5.15-6 Real-World Calendar Source (POST-M6.0-E1-P1-R2)
# icalendar (PyPI MIT): VEVENT parsing - Calendar.from_ical + calendar.walk('VEVENT')
# Tested with icalendar 7.2.2; supports Python 3.10+ (production hermes-agent venv is 3.11.x)
icalendar>=7.0
