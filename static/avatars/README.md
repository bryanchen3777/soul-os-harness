# static/avatars/

放頭像圖檔（可選，UI 會 fallback 到 initials 圓形）

命名格式：
  yua.png     — Yua 的頭像
  ruka.png    — Ruka 的頭像
  akane.png   — Akane 的頭像
  bryan.png   — Bryan（你自己）的頭像

格式：PNG 或 JPG，尺寸建議 128x128 以上。
如果沒有放，UI 會自動用 initials fallback（彩色圓形 + 第一個字）。
